@model IEnumerable<taskjo.Models.Project>

@{
    Layout = "~/Views/Shared/DefaultLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.teams.teamName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.projectName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.projectDesc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.projectdate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.projectSate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.projectDocFile)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.teams.teamName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.projectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.projectDesc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.projectdate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.projectSate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.projectDocFile)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.projectId }) |
                @Html.ActionLink("Details", "Details", new { id=item.projectId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.projectId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
