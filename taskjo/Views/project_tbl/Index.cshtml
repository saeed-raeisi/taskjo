@model IEnumerable<taskjo.Models.project_tbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.group.groupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.projectAdmin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.projectName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.des)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.group.groupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.projectAdmin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.projectName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.des)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.projectId }) |
            @Html.ActionLink("Details", "Details", new { id=item.projectId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.projectId })
        </td>
    </tr>
}

</table>
